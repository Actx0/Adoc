---
title: "Get Prompt by Name"
description: "Fetch a prompt version by handle. Optional `?version=` query."
---

Fetch a prompt version by handle. Optional `?version=` query.

## Endpoint

- **Method**: `GET`
- **URL**: `/api/v1/workspaces/{workspaceId}/promptsByName/{promptName}`

## Required headers

| Header | Required | Description |
| --- | --- | --- |
| `X-Access-Key: <ACCESS_KEY>` | Yes* | Workspace access key. Session cookie or `X-API-Key` also work when the user has permission. |
| `Accept: application/json` | Yes | Ensures a JSON response. |
