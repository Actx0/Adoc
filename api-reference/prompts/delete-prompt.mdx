---
title: "Delete Prompt"
description: "Delete a prompt and all versions."
---

Delete a prompt and all versions.

## Endpoint

- **Method**: `DELETE`
- **URL**: `/api/v1/workspaces/{workspaceId}/prompts/{promptId}`

## Required headers

| Header | Required | Description |
| --- | --- | --- |
| `X-Access-Key: <ACCESS_KEY>` | Yes* | Workspace access key. Session cookie or `X-API-Key` also work when the user has permission. |
| `Accept: application/json` | Yes | Ensures a JSON response. |

## Response

<CodeGroup>
```json Response
{ }
```
</CodeGroup>
